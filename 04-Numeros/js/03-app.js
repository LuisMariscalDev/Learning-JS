
let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.8);
resultado = Math.round(2.2);
resultado = Math.round(2.6);
resultado = Math.round(2.5);
resultado = Math.round(2.4);

// Redondear hacia arriba
resultado = Math.ceil(3.1);

// Redondear hacia abajo
resultado = Math.floor(4.9);

// Raiz Cuadrada
resultado = Math.sqrt(81);

// Potencia
resultado = Math.pow(2, 8);

// Absoluto
resultado = Math.abs(-566);

// Minimo
resultado = Math.min(1, 45, 52, -2);

// Maximo
resultado = Math.max(25, 45, 633, -45);

// Aleatorio
resultado = Math.random() * 20;

// Aleatorio dentro de un rango
resultado = Math.floor(Math.random() * 120);


console.log(resultado);