const autenticado = true;

/*
if(autenticado) {
    console.log('Sí puede ver Netflix');
} else {
    console.log('No puedes ver pelis bb')
}
 */

// Operador Ternario

console.log( autenticado ? 'Sí está autenticado' : 'No está autenticado');
