const producto = 'Tablet';

// No se puede reasignar el valor cuando se trabaja con const
// prodcuto = "monitor";

console.log(producto);

// No se puede iniciar una variable sin valor cuando se trabaja con const
const precio = 256;
console.log(precio);